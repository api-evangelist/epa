naftiko: 1.0.0-alpha2
info:
  label: Monitor Plan Management OpenAPI Specification — Spans
  description: 'Spans. 1 operations. Lead operation: operations. Self-contained Naftiko capability covering one EPA business surface.'
  tags:
    - EPA
    - Spans
  created: '2026-05-29'
  modified: '2026-05-29'
binds:
  - namespace: env
    keys:
      EPA_APIKEY_KEY: EPA_APIKEY_KEY
capability:
  consumes:
    - type: http
      namespace: cam-monitor-plan-spans
      baseUri: https://api.epa.gov/easey
      description: Monitor Plan Management OpenAPI Specification — Spans business capability. Self-contained, no shared references.
      authentication:
        type: apikey
        key: x-api-key
        value: '{{env.EPA_APIKEY_KEY}}'
        placement: header
      resources:
        - name: monitor-plan-mgmt-locations-locid-spans
          path: /monitor-plan-mgmt/locations/{locId}/spans
          operations:
            - name: monitorSpanControllergetSpans
              method: GET
              description: monitorSpanControllergetSpans
              inputParameters:
                - name: locId
                  in: path
                  type: string
                  required: true
                  description: locId
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: $.
  exposes:
    - type: rest
      namespace: cam-monitor-plan-spans-rest
      port: 8080
      description: REST adapter for Monitor Plan Management OpenAPI Specification — Spans. One Spectral-compliant resource per consumed operation, prefixed with /v1.
      resources:
        - path: /v1/monitor-plan-mgmt/locations/{locid}/spans
          name: monitor-plan-mgmt-locations-locid-spans
          description: REST surface for monitorSpanControllergetSpans.
          operations:
            - method: GET
              name: monitorSpanControllergetSpans
              description: monitorSpanControllergetSpans
              call: cam-monitor-plan-spans.monitorSpanControllergetSpans
              with:
                locId: rest.locId
              outputParameters:
                - type: object
                  mapping: $.
    - type: mcp
      namespace: cam-monitor-plan-spans-mcp
      port: 9090
      transport: http
      description: MCP adapter for Monitor Plan Management OpenAPI Specification — Spans. One tool per consumed operation, routed inline through this capability's consumes block.
      tools:
        - name: monitorspancontroller-getspans
          description: monitorspancontroller-getspans
          hints:
            readOnly: true
            destructive: false
            idempotent: true
          call: cam-monitor-plan-spans.monitorSpanControllergetSpans
          with:
            locId: tools.locId
          outputParameters:
            - type: object
              mapping: $.
